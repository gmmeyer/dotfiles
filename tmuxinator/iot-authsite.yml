# ~/.tmuxinator/iot-authsite.yml

name: iot-authsite
root: ~/dev/iot-authsite

windows:
  - workspace:
  - editor:
  - server:
      layout: even-horizontal
      panes:
        - python manage.py runserver localhost:8015
        - npm run build
