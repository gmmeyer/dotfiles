# ~/.tmuxinator/flow-iot-site.yml

name: flow-iot-site
root: ~/dev/flow-iot-site

windows:
  - workspace:
  - editor:
  - server:
      layout: even-horizontal
      panes:
        - npm run start_reload_db
        - npm run watch
