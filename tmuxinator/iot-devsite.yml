# ~/.tmuxinator/iot-devsite.yml

name: iot-devsite
root: ~/dev/iot-devsite

windows:
  - workspace:
  - editor: e .
  - server:
      layout: even-horizontal
      panes:
        - npm run server
        - npm run build && npm run watch
