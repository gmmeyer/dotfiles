# ~/.tmuxinator/bbh-dashboards.yml

name: bbh-dashboards
root: ~/dev/bbh-dashboards

windows:
  - workspace:
  - editor:
  - server:
      layout: even-horizontal
      panes:
        - npm run start
        - npm run build && npm run watch
