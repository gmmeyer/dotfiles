# ~/.tmuxinator/landing-pages.yml

name: landing-pages
root: ~/dev/landing-pages

windows:
  - workspace:
  - editor:
  - server:
      layout: even-horizontal
      panes:
        - npm run start
        - # npm run watc
