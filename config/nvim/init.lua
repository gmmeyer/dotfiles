-- Neovim config. Plugins are managed by lazy.nvim (auto-bootstrapped on
-- first launch); run :Lazy to manage them and :checkhealth if anything acts up.
require('config.options')
require('config.keymaps')
require('config.lazy')
